# DEPRECATED

This Express backend is superseded by:

- `apps/auth-client` — OAuth
- `apps/mail-host` — mail APIs + inbox
- `packages/db`, `packages/mail` — shared libraries

Kept for reference until Vercel cutover. See `docs/VERCEL_DUAL_APPS.md`.
